spring:
  application:
    name: demo
  datasource:
    url: jdbc:mariadb://localhost:3306/demo
    username: demo
    password: demo
    driver-class-name: org.mariadb.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
